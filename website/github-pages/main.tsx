import React from "react";
import ReactDOM from "react-dom/client";
import OccamySite from "../app/page";
import "../app/globals.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <OccamySite />
  </React.StrictMode>,
);
